TODO:
	-Добавить обработчик библиотеки (включая проверочку)
		ДОПИСАТЬ, ТАМ НЕМНОГО ОСТАЛОСЬ

	-Убрать reg, добавить inout, assign

	-Обратный транслятор T_T
	
	-Нормальная функция поиска/вывода информации об элемента
	
	-Функция вывода всех элементов, элементов типа, типов.
	
	-Замена элементов
		-Потом ещё и обработку XML нужно прифигачить
	
	-Можно попробовать добавить поддержку нескольких струтур данных, с независимыми пространствами имён.
		Ну типа так: сканирует парсер код, один модуль закончился, парсер выдеряет память под новый массив, и другой модуль парсит в
		новый массив, имена теперь могут совпадать, все довольны, все радуются.

Список ФИЧЕЙ:
	-Ограниченный размер списка чуть менее 10к
	-Непонятно как это будет работать, если будут разные модули и в них элементы с одним именем
	-Непонятно, что делать когда удаляешь элемент (переименовать, я думаю, но таблица-то на хэшах!)

DONE:
	-Добавить функцию, связывающую провода, входы, выходы с подключенными к ним элементами

	-Добавить отслеживание строки, на которой был введен элемент (может быть полезно при обратной трансляции)